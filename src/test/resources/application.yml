spring:
  datasource:
    url: jdbc:h2:mem:orders;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

app:
  xsd:
    purchase-order: classpath:xsd/purchase-order.xsd
  xslt:
    invoice: classpath:xslt/invoice.xslt
